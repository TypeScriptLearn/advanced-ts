const title = 'Learning Generics and Decorators in TypeScript';

const header = document.createElement('h1');
header.style.textAlign = 'center';
header.textContent = title;
document.body.appendChild(header);
